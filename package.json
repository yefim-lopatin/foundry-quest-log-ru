{
  "name": "foundry-quest-log-ru",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "PF2e Journal для Foundry VTT v14 с начальным набором «Растхендж»",
  "scripts": {
    "bundle": "esbuild scripts/main.js --bundle --format=esm --target=es2022 --sourcemap --outfile=index.js",
    "test": "npm run bundle && node --test && node tools/validate.mjs",
    "build": "npm run bundle && node tools/build.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
